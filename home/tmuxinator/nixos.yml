# /home/pjjimiso/.config/tmuxinator/nixos.yml

name: nixos
root: ~/Projects/nixos
on_project_start: export PATH=$PATH:~/.local/bin

windows:
  - claude:
      layout: even-horizontal
      panes:
        - claude
